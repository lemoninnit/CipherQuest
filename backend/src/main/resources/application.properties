# ==============================
# CipherQuest – Application Config
# ==============================

# Server
server.port=8080

# Supabase PostgreSQL Database Connection (Using IPv4 Compatible Connection Pooler)
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres.jacbtscvbixzsfbkkjcf
spring.datasource.password=Lenonlee@7312005

# JPA / Hibernate Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=CipherQuestSuperSecretKey2026ShouldBeAtLeast256BitsLongForHS256Algorithm
jwt.expiration-ms=86400000

# CORS – update origin for production
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Fishing game config
game.fishing.max-attempts=10
game.fishing.base-xp=100
game.fishing.streak-bonus-multiplier=1.5
